program Programa;
// comentário de linha
const nomeConstante = 4;
var minhaVariavel: string;
procedure Soma (a, b: integer);
begin
    resultado := (10 + 5) * 2; // saída: 30
    print {a + b};
end;
/*
comentário de bloco
*/
procedure Saudacao(nome, sobrenome: string);
begin
 print { 'Olá, ', nome, sobrenome };
end;

procedure Saudacao(nome: string, idade: integer, altura: real);
begin
    if idade > 18 then
        begin
            print { 'Maior de idade' };
        end
    print { 'Olá, ', nome};
    print { 'Sua idade: ', idade };
    print { 'Sua altura: ', altura };
end;

procedure ContarAte5;
begin
    for i := 1 to i = 5 do
        begin
            print { 'Número: ', i };
            i := i + 1;
        end
end;


begin
    Saudacao('Maria', 'da Silva', 20);
end.